import 'package:flutter/material.dart';

class TodayDiscoveryFragment extends StatelessWidget {
  const TodayDiscoveryFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      height: 700,
    );
  }
}
